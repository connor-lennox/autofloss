import {PixelImage} from "../ImageParser";

export const solvePattern = (image: PixelImage, maxColors: number) => {
    console.log("Finding solution for image with " + image.height + " rows and " + image.width + " columns.")
}
